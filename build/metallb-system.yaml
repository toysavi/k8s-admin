apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: single-ip-pool
  namespace: metallb-system
spec:
  addresses:
  - 10.0.0.5/32
---
apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: single-ip-adv
  namespace: metallb-system
spec:
  ipAddressPools:
  - single-ip-pool
